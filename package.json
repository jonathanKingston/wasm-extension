{
  "name": "wasm-test",
  "version": "1.0.0",
  "description": "",
  "main": ".",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "build": "rm -rf src/pkg && cd rust-component && wasm-pack build -t no-modules -d ../src/pkg/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jonathan Kingston",
  "license": "MPL-2.0",
  "dependencies": {
  },
  "devDependencies": {
  }
}
